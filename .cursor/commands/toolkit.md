# List Cursor toolkit

Inventory this project's Cursor setup.

## Scan

- `.cursor/commands/*.md` — slash commands
- `.cursor/rules/*.mdc` — rules
- `.cursor/skills/**/SKILL.md` — skills
- `.cursor/docs/` — documentation

Also note if I have global copies under `~/.cursor/commands/` or `~/.cursor/skills/` relevant to the same names.

## Output

Markdown table per category: **name** | **path** | **one-line purpose**

End with: point to `.cursor/docs/USER-MANUAL.md` and native Cursor **`/rules`** / **`/commands`** where relevant.
