# Write a slash command

Use the **write-cursor-commands** skill (`.cursor/skills/write-cursor-commands/`).

## Your job

1. Clarify: command name, project vs global (`~/.cursor/commands/`), and what the workflow should do.
2. Create `<name>.md` with a clear `#` title and imperative steps.
3. Confirm how I invoke it: `/command-name`.
4. If this command belongs in the user manual, add or update a row in `.cursor/docs/USER-MANUAL.md` under Project commands.

If I already specified the command in this message, implement it without extra questions.
