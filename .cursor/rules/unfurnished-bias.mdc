---
description: This pack is on — pick the slot, do not dump the catalog
alwaysApply: true
---

If `scratch/cursormax-off` exists, or this turn is clearly driven by another workflow pack (user attached poteto-mode / typed `/poteto-mode`), pack slots are off (skip sdd, sdd-eng, verify, grill, blueprint, ticket, after-compact, thermonuclear, pre-flight, evals, write-skill). yagni-bias stays. One-line edit: no skill, do not probe the file. Else this pack is on. File on disk → sdd-eng and tdd; patch in place, never blank-replace a living durable file. Mint a durable file → sdd. No checkable done-line → grill. Kickoff → blueprint. Multi-file done → verify. Ship → pre-flight. Do not dump the catalog.
