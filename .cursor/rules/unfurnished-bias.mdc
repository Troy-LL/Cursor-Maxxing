---
description: Maximize Cursor natives, one accurate slot, no extra probes
alwaysApply: true
---

If `scratch/unfurnished-off` or `scratch/cursormax-off` exists, pack slots are off (skip sdd, sdd-eng, verify, grill, blueprint, ticket, after-compact, thermonuclear, pre-flight, evals, write-skill). yagni-bias stays. If they attached another pack's skill this turn, skip those slots too, do not pull tdd, and do not wrap their tools. Else this pack is on, alongside other plugins. Maximize Cursor natives (Grep, Glob, Read, Plan, Task, Debug). Do not add a tool or MCP the IDE ships. Do not extra-probe. If `AGENTS.md` exists, Read it before a repo-wide Grep or Glob. Honor a skip or do not open line. One-line edit: no skill, do not probe the file. No checkable done-line → grill. Kickoff → blueprint. File on disk → sdd-eng and tdd; patch in place, never blank-replace a living durable file. Mint a durable file → sdd. Multi-file done → verify. Ship → pre-flight. Do not dump the catalog. Do not fight another pack for the same job. Do not bullet `scratch/` or `scratch/tickets/` in AGENTS.md, even if they asked; do not delete a do-not-map line to satisfy that; do not offer to rewrite it.
